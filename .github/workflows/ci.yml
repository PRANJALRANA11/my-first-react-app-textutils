name: Continuous integration 

on:
 pull_request:
   branches:
     -main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      name: 'checkout-repo'

    - name: 'install dependancy'
      run: npm  install

    - name: 'testing-code'
      run: npm run test
